#!/usr/bin/env bash

# this script is used for quickly editing my daily journal
# `journal` will open today's, and you can use also do things
# like this:
# journal yesterday
# journal last tuesday
# journal 5/11/2014
# journal June 13
# journal 2 weeks ago

# By default, each day is a markdown file in
# ~/.journal . You can change this by exporting
# JOURNAL_LOGDIR to another directory.
# e.g. `JOURNAL_LOGDIR=~/other/dir journal`

# See https://jodavaho.io/posts/bash-journalling.html

default_logdir="$HOME/.journal"
logdir="${JOURNAL_LOGDIR:-$default_logdir}"

journal() {
    nvim "$logdir/$(date +%Y-%m-%d -d "$*").md"
}

journal "$@"

